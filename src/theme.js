import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: `"SF Pro Display", "Helvetica", "Arial", sans-serif`,
    fontWeightBold: 700, // SF Pro Bold weight
  },
});

export default theme;
